# Auto detect text files and perform LF normalization
* text=auto

# Treat all image formats as binary — prevents git from mangling bytes
*.jpg binary
*.jpeg binary
*.png binary
*.webp binary
*.gif binary
*.svg text eol=lf
